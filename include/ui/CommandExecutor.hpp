#include <tr1/memory>
#include <bits/stdc++.h>
#include <string>
#include <boost/algorithm/string.hpp>
using namespace std;

class CommandExecutor {
  static std::string executeCommand();
  static std::string executeCommand(std::string commandInput);
}
